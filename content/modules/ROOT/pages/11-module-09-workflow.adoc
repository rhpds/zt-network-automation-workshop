= 2-5: Creating a Workflow
:source-highlighter: rouge
:toc: macro
:toclevels: 1

This module demonstrates the use of link:https://docs.redhat.com/en/documentation/red_hat_ansible_automation_platform/latest/html/using_automation_execution/controller-workflow-job-templates[Ansible Automation Platform workflows^]. Workflows allow you to configure a sequence of disparate job templates (or workflow templates) that may or may not share inventory, playbooks, or permissions.

For this exercise we will create a time-stamped backup. If the backup job successfully completes, the workflow will simultaneously configure a banner and a user. If either job template fails we will restore to the time-stamped backup.

== Learning objectives

By the end of this module, you will be able to:

* Create a Workflow Template that chains multiple job templates together
* Use the Workflow Visualizer to build and connect workflow nodes
* Configure success and failure paths to implement automated rollback
* Create converged links between workflow nodes
* Launch and monitor a workflow execution

== Step 1: Create a workflow template

. Make sure you are logged in as the **{controller_user}** user.

. Click on the **Templates** link on the left menu.
+
// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/templates.png
image::templates.png[Templates link,align="center",link=self,window=blank,width=600]

. Click on the blue **Create template** button and select **Create workflow job template**.
+
// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/controller_add_workflow.png
image::controller_add_workflow.png[Add workflow template button,align="center",link=self,window=blank,width=600]

. Fill out the form as follows:
+
[cols="1,1",options="header"]
|===
| Parameter | Value
| Name | Workshop Workflow
| Organization | Red Hat network organization
| Inventory | Workshop Inventory
|===

. Click on the blue **Create workflow job template** button

== Step 2: The Workflow Visualizer

. When you clicked the **Create workflow job template** the **Workflow visualizer** should automatically open. If not, click on the **View workflow visualizer** button.
+
// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/visualizer_tab.png
image::visualizer_tab.png[Visualizer tab link,align="center",link=self,window=blank,width=600]

. By default only a blue **Add step** button will appear. Click on the **Add step** button.
+
// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/add_step.png
image::add_step.png[Add step button,align="center",link=self,window=blank,width=600]

. The **Add step** window will appear.
+
* Set the Node Type to `Job Template`.
* Select the `Backup network configurations` Job Template that was created in exercise 6.
* Convergence can be left as **Any**
* Node alias can be left blank
+
// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/add_backup_node.png
image::add_backup_node.png[Add backup node,align="center",link=self,window=blank,width=600]
+
* Click the blue **Next** button.
* Click on the blue **Finish** button

NOTE: The `Backup network configurations` job template is now a node. Job or workflow templates are linked together using a graph-like structure called nodes. These nodes can be approvals, jobs, project syncs, inventory syncs, or even other workflows. A template can be part of different workflows or used multiple times in the same workflow.

// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/step2_workflow.png
image::step2_workflow.png[Backup node configured,align="center",link=self,window=blank,width=600]

== Step 3: Add the Configure Banner Job Template

. Hover over the three dots on the `Backup network configurations` node and click the **Add step and link** link.
+
// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/new_add_step.png
image::new_add_step.png[Add step link,align="center",link=self,window=blank,width=600]
+
The **Add Step** window will appear again.

. Fill out the following values:
+
[cols="1,1",options="header"]
|===
| Parameter | Value
| Node Type | Job Template
| Job template | Network-Banner
| Status | Run on success
| Convergence | Any
| Node alias |
|===
+
// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/step3_add_node.png
image::step3_add_node.png[Add second node,align="center",link=self,window=blank,width=600]

NOTE: Workflows can be configured to run automation jobs when the previous node succeeds, fails, or have it always run no matter what the previous job did. This allows workflows to fix issues or revert the state of a device.

. Click the blue **Next** button and fill out the survey field
+
// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/step3_add_network_banner.png
image::step3_add_network_banner.png[Add network banner job template,align="center",link=self,window=blank,width=600]

. Click the blue **Next** button again, review, and then click the blue **Finish** button.

. A green line should exist between `Backup network configurations` and `Network-Banner`
+
// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/step3_final.png
image::step3_final.png[Banner node,align="center",link=self,window=blank,width=600]

== Step 4: Add the Configure Network-User Job Template

. Hover over the three dots on the `Backup network configurations` node and click the **Add step and link** link. (not the `Network-Banner` node)

. Fill out the following values:
+
[cols="1,1",options="header"]
|===
| Parameter | Value
| Node Type | Job Template
| Job template | Network-User
| Status | Run on success
| Convergence | Any
| Node alias |
|===
+
// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/step4_add_node.png
image::step4_add_node.png[Select network user job,align="center",link=self,window=blank,width=600]

. Click the blue **Next** button and fill out the survey field (feel free to leave the defaults!)
+
// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/user-survey.png
image::user-survey.png[User survey,align="center",link=self,window=blank,width=600]

. Click **Next**, review then click **Finish**
+
Your workflow should now look similar to the following image:
+
// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/step4_final.png
image::step4_final.png[Configure user node,align="center",link=self,window=blank,width=600]

== Step 5: Add the Network-Restore Job Template

. Hover over the **Network-Banner** node and click the three dots. The **Add step and link** window will appear again.

. Fill out the following values:
+
[cols="1,1",options="header"]
|===
| Parameter | Value
| Node Type | Job Template
| Job template | Network Automation - Restore
| Status | Run on fail
| Convergence | Any
| Node alias |
|===
+
Your form should look similar to the following image:
+
// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/fill_out_restore.png
image::fill_out_restore.png[Restore form,align="center",link=self,window=blank,width=600]

. Click the blue **Next** button for the Survey step, then choose a rollback date (there may only be one choice if you only ran the backup one time)

. Click the blue **Next** button again, then review and click the **Finish** button.
+
Your workflow should now look similar to the following image:
+
// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/step5_final.png
image::step5_final.png[Configure restore node,align="center",link=self,window=blank,width=600]

== Step 6: Create a converged link

. Hover over the `Network-User` node until a small arrow appears to the right.
+
// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/arrow.png
image::arrow.png[Arrow,align="center",link=self,window=blank,width=600]

. Click on the arrow (it will turn gray), and drag it over to the `Network Automation - Restore` node
+
// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/step6_on_fail.png
image::step6_on_fail.png[On fail link,align="center",link=self,window=blank,width=600]

. Now click on the **Run always** and change to **Run on fail**
+
// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/step6_complete_workflow.png
image::step6_complete_workflow.png[Complete workflow,align="center",link=self,window=blank,width=600]

. Click Save, then click on the **X** to exit the visualizer.
+
// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/save.png
image::save.png[Save button,align="center",link=self,window=blank,width=600]

== Step 7: Run the Workflow

. Click the **Launch** button.
+
// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/step7_launch.png
image::step7_launch.png[Launch workflow,align="center",link=self,window=blank,width=600]

. Watch the **Workshop Workflow**.
+
// TODO: Copy image from ansible-workshops/exercises/ansible_network/9-controller-workflow/images/step7_final.png
image::step7_final.png[Workflow job launched,align="center",link=self,window=blank,width=600]
+
At any time during the workflow job you can select an individual job template by clicking on the node to see the status.

== Module summary

In this module, you successfully:

* Created a **Workflow template** that creates a backup, then attempts to create a user and banner for all network nodes
* Made the workflow **robust** -- if either job template fails it will restore to the specified backup
* Launched the Workflow template and explored the **Workflow Visualizer**
